# test_vanillaPlugin

 - test enable page (firefox) : `about:debugging#/runtime/this-firefox`
 - charger un module temporaire (valable jusqu'à fermeture de firefox) -> manifest.json (ou tout autre fichier dans le folder du plugin)
 - éventuellement clique sur actualiser
 - go sur youtube.com